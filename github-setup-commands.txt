# GitHub Setup Commands
# After creating your repository on GitHub, run these commands:

# Add the remote repository (replace YOUR_USERNAME with your GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/cloud-storage-platform.git

# Push your code to GitHub
git branch -M main
git push -u origin main

# Example with your username (nirdoshprasher004):
# git remote add origin https://github.com/nirdoshprasher004/cloud-storage-platform.git
# git branch -M main
# git push -u origin main